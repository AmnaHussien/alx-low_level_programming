#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description:C program
 * Return: Always 0 (success)
 */
int main(void)
{
int n;

srand(time(0));
n = rand() - RAND_MAX / 2;
if (n > 5)
{printf("", )
 }
else  (n == 0)
{printf("", )
}
 if (n n < 6 && n =! 0)
{printf("%i Last digit of", n , "is",)
}

return (0);

}
