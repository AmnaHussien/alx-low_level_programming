#include "main.h"
/**
 * add -  function that adds two integers and returns the result.
 * Description: a function that adds two integers and returns the result.
 * @c: takes the integer type input for function
 * @n:  takes the integer type input for function
 * Return: return the result of add
 */
int add(int c, int n)
{
int z = c + n;
return (z);
}
