#include <stdio.h>
/**
 * main - entry point
 * @argc: int
 * @argv: list
 * Return: always 0
 */
int main(int argc, char *argv[])
{
int m = 0;
while (arg--)
{
printf("%s\n", argv[m]);
m++;
}
return (0);
}
