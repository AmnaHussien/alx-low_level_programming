#include "main.h"
/**
 * _strlen - function that returns the length of a string
 * Description: C programm
 * @s: character input types
 * Return: Always 0 (success)
 */
int _strlen(char *s)
{
char counter;
for (counter = *s[0]; counter != '\0'; counter++)
{
return (counter);
}
}
